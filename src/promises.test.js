const getRandomValue = require('./get-random-value');

describe('getRandomValue', () => {
	xit('returns a promise of a random integer', () => {

	});
});


describe('getTwoRandomValues', () => {
	const getTwoRandomValues = (min, max) => {};

	xit('returns an array of two integers', () => {

	});
});
